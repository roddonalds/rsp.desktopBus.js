window.addEventListener('load', () => {
    
    /*const dsocketClient = window.rsp.dsocketClient;

    dsocketClient.emit('MySignal', { data: 'Hello, world!' });

    dsocketClient.execute('some_script.sh');

    dsocketClient.listen('MySignal', (event) => {
        console.log('Received signal:', event.detail);
    });

    dsocketClient.listen('executionResult', (event) => {
        console.log('Execution result:', event.detail);
    });

    dsocketClient.listen('executionError', (event) => {
        console.error('Execution error:', event.detail);
    });*/

});